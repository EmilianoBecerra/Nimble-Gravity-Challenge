export interface User {
  uuid: string,
  candidateId: string,
  applicationId: string,
  firstName: string,
  lastname: string,
  email: string
}

export interface Jobs {
  id: string,
  title: string
}